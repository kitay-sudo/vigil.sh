# Security Monitor - Список изменений

## v2.2 - HTTP API для статистики и мониторинга

### 🚀 HTTP API Endpoints

Добавлен HTTP API сервер для получения статистики в формате JSON:

**Endpoints:**
- `GET /health` - healthcheck (uptime, статус)
- `GET /stats` - статистика (блокировки, угрозы, топ стран)
- `GET /status` - полный статус (конфиг, защита, whitelist)

**Настройка:**
```json
{
  "api": {
    "enabled": true,
    "host": "0.0.0.0",
    "port": 8765
  }
}
```

**Использование:**
```bash
# Healthcheck
curl http://localhost:8765/health

# Статистика
curl http://localhost:8765/stats | jq

# Полный статус
curl http://localhost:8765/status | jq
```

**Интеграция с nginx:**
Можно проксировать через nginx для доступа по домену:
```nginx
location /api/ {
    proxy_pass http://localhost:8765/;
}
```

**Безопасность:**
- Данные только для чтения (read-only API)
- Можно ограничить доступ через iptables или host в конфиге
- Рекомендуется `"host": "127.0.0.1"` для localhost-only

### Применение:
- Мониторинг uptime (healthchecks)
- Получение статистики для дашбордов
- Интеграция с системами мониторинга
- Автоматизация и скрипты

---

## v2.1 - Опциональный SSH мониторинг

## Новые возможности

### 1. Опциональный мониторинг SSH

Теперь можно включить или выключить мониторинг SSH при установке или в конфигурации:

```json
{
  "enable_ssh_monitoring": true,  // или false для отключения
  "ssh_port": 22                   // ваш порт SSH
}
```

**Зачем это нужно?**
- Если вы отключили парольную аутентификацию SSH и используете только ключи
- SSH брутфорс становится невозможен
- Можно отключить мониторинг SSH и оставить только защиту от DDoS/флуда

### 2. Автоопределение порта SSH

Скрипт установки автоматически определяет ваш SSH порт:
- Проверяет запущенные SSH сервисы
- Предлагает использовать обнаруженный порт
- Можно указать другой порт вручную

### 3. Система белых списков

**Уже реализовано!** Два типа белых списков:

#### SSH Whitelist (Админские IP)
```json
{
  "ssh_whitelist": [
    "94.241.174.106",
    "YOUR_HOME_IP"
  ]
}
```
- IP из этого списка **никогда не будут заблокированы** за SSH активность
- Используется для ваших личных IP адресов
- Позволяет безопасно администрировать сервер

#### Service Whitelist (Внутренние сервисы)
```json
{
  "service_whitelist": [
    "127.0.0.1",
    "89.223.64.38",    // MongoDB сервер
    "10.0.0.5"         // Redis сервер
  ]
}
```
- IP из этого списка **никогда не будут заблокированы**
- Используется для внутренних сервисов (MongoDB, Redis, API серверы)
- Предотвращает блокировку ваших собственных сервисов

### 4. Гибкая защита

Можно настроить, что блокировать, а что только мониторить:

```json
{
  "protection": {
    "block_ssh_bruteforce": false,     // выкл если используете только ключи
    "block_unknown_ssh": false,        // выкл если используете только ключи
    "block_syn_flood": true,           // всегда вкл - защита от DDoS
    "block_excessive_http": false,     // обычно выкл - не блокируем пользователей
    "notify_excessive_http": true      // но уведомляем о высокой нагрузке
  }
}
```

## Как использовать

### При установке
Просто запустите `./install.sh` и ответьте на вопросы:
- Хотите ли включить SSH мониторинг?
- Какой порт SSH используется?
- Какие IP добавить в SSH whitelist?
- Какие IP добавить в Service whitelist?

### После установки

#### Добавить IP в SSH whitelist:
```bash
security-monitor --add-ssh-whitelist 94.241.174.106
```

#### Добавить IP в Service whitelist:
```bash
security-monitor --add-service-whitelist 89.223.64.38
```

#### Посмотреть статус:
```bash
security-monitor --show-status
```

#### Отредактировать конфиг:
```bash
nano /opt/security-monitor/config.json
systemctl restart security-monitor
```

## Рекомендуемая конфигурация

### Для серверов с SSH по ключам (без паролей):
```json
{
  "enable_ssh_monitoring": false,
  "ssh_whitelist": ["YOUR_IP"],
  "service_whitelist": ["127.0.0.1", "YOUR_INTERNAL_IPS"],
  "protection": {
    "block_ssh_bruteforce": false,
    "block_unknown_ssh": false,
    "block_syn_flood": true,
    "block_excessive_http": false,
    "notify_excessive_http": true
  }
}
```

### Для серверов с парольным SSH:
```json
{
  "enable_ssh_monitoring": true,
  "ssh_port": 22,
  "ssh_whitelist": ["YOUR_IP"],
  "service_whitelist": ["127.0.0.1", "YOUR_INTERNAL_IPS"],
  "protection": {
    "block_ssh_bruteforce": true,
    "block_unknown_ssh": true,
    "block_syn_flood": true,
    "block_excessive_http": false,
    "notify_excessive_http": true
  }
}
```

## Важно!

- **SSH Whitelist** - защищает от случайной блокировки ваших IP
- **Service Whitelist** - защищает ваши сервисы от блокировки
- IP из любого whitelist **никогда не блокируются**
- Localhost (127.0.0.1) **всегда** в whitelist автоматически
- Можно отключить SSH мониторинг, если используете только ключи
- SYN flood защита **всегда** активна (это DDoS атака, не обычный трафик)
